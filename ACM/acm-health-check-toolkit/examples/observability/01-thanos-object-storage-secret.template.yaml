# TEMPLATE ONLY. Do not commit real credentials to Git.
# Prefer an enterprise secret manager, External Secrets, or Sealed Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: thanos-object-storage
  namespace: open-cluster-management-observability
type: Opaque
stringData:
  thanos.yaml: |
    type: s3
    config:
      bucket: REPLACE_WITH_BUCKET
      endpoint: REPLACE_WITH_ENDPOINT_WITHOUT_PROTOCOL
      insecure: false
      access_key: REPLACE_WITH_ACCESS_KEY
      secret_key: REPLACE_WITH_SECRET_KEY
      # For a private CA, mount the CA through the supported MCO TLS secret
      # configuration and add http_config.tls_config. Do not set
      # insecure_skip_verify: true in production.
